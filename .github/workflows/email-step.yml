name: Email Step Demo

on:
  push:
    branches:
      - main  # Or your default branch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: 23f2001697@ds.study.iitm.ac.in
        run: echo "Hello, world!"
